(ns web.styles)


; Color
(def c-primary "#BE1414")

(def c-typography "#333")

; Font Family
(def sans-serif
  {:fontFamily "-apple-system, BlinkMacSystemFont,
                 'avenir next', avenir,
                 helvetica, 'helvetica neue',
                 ubuntu,
                 roboto, noto,
                 'segoe ui', arial,
                 sans-serif"})

(def serif
  {:fontFamily "georgia,
                 times,
                 serif"})

; Type Scale
(def f1
  {:fontSize "3rem"})

(def f2
  {:fontSize "2.25rem"})

(def f3
  {:fontSize "1.5rem"})

(def f4
  {:font-size "1.25rem"})

(def f5
  {:fontSize "1rem"})

(def f6
  {:fontSize ".875rem"})
